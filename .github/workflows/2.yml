name: Test two
on: [push]

jobs:
    Initiate:
      runs-on: [ubuntu-latest]
      
      steps:
      - uses: actions/checkout@v2
      - name: action two
      
        run: echo "hello $GITHUB_ACTOR"
